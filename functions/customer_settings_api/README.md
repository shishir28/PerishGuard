# customer_settings_api

`GET /api/customer-settings`

Returns the active customer’s effective thresholds and alert settings.

`PUT /api/customer-settings`

Updates the active customer’s runtime settings.
